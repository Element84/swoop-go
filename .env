# postgres vars
# # using 127.0.0.1 instead of localhost because (co)lima
# hasn't implemented ipv6 port forwarding yet
SWOOP_DATABASE_HOST="127.0.0.1"
SWOOP_DATABASE_PORT="5432"
SWOOP_DATABASE_USER="postgres"
SWOOP_DATABASE_PASSWORD="password"
SWOOP_DATABASE_NAME="swoop"
SWOOP_DATABASE_URL_EXTRA="?sslmode=disable"

# minio vars
AWS_ACCESS_KEY_ID="minio"
AWS_SECRET_ACCESS_KEY="password"

# swoop vars
SWOOP_S3_ENDPOINT="http://127.0.0.1:9000"
SWOOP_BUCKET_NAME="swoop"
SWOOP_EXECUTION_DIR="s3://${SWOOP_BUCKET_NAME}/execution"
